'use strict'

/**
 * Use this file to store any deployment-varying variables.
 * Obviously in production we would store the API key in a cookie and retrieve
 * it from the server using a user-provided secret, but hey it's a quick project
 */

/**
 * string : The base URL of the OpenWeatherMap API
 */
export const baseUrl =  'http://api.openweathermap.org/data/2.5';

/**
 * string : The API key to use with the OpenWeatherMap API
 */
export const apiKey = '23d74f0c5b5263d9a0541ed130547f64';
